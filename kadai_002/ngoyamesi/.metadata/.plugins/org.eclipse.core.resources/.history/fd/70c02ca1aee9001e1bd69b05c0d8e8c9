package com.example.nagoyamesi.service;

import java.time.LocalDate;

public class ReservationService {
	public static void main(String[] args) {  
	    LocalDate timeNow = LocalDate.now();  // Create a date object
	    System.out.println(timeNow);  // Display the current date
	  }  
	
	// 2時間以上の空きがある
	 public boolean isWithinCapacity(String fromReservationTime, String capacity) {
         return fromReservationTime <= Today;
     }
	
}
